# Copy this file into any repo as .github/workflows/aria-qa.yml
name: ARIA QA

on:
  pull_request:
    branches: [main, master]
    types: [opened, synchronize, reopened]
  push:
    branches: [main, master]

jobs:
  aria-qa:
    uses: MOD-Ventures-Org/QA-Agent/.github/workflows/qa-pipeline.yml@master
    secrets: inherit
